
name: cowpy exmaple

on:
  push:

jobs:
  install-cowpy:
    runs-on: ubuntu-latest
    steps:
    - name: install-cowpy
      run: pip install cowpy
    - name: run-cowpy
      run: cowpy -c tux hi there